############################
# WORKSPACES
#
# Workspace->monitor bindings live in the hyprdynamicmonitors profiles
# (~/.config/hyprdynamicmonitors/hyprconfigs/{docked,undocked}.go.tmpl),
# because only one monitor is ever active at a time and the active monitor
# differs per profile. Defining them here caused duplicate/conflicting rules.
############################
